export const enum Turn {
	BLUE = 100,
	RED = 200
}

export const enum Phase {
	BAN = 'ban',
	PICK = 'pick',
	SIDE = 'side',
	DONE = 'done',
	NONE = 'none'
}

export const enum UserRole {
	BLUE = 100,
	RED = 200,
	SPECTATOR = 'spectator'
}
